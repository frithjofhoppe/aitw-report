\section{Traceability Architektur}
\label{sec:traceability-architecture}

\subsection{Grundarchitektur}

% \hl{structure notes start}
% \begin{itemize}
%     \item Basismodell / Subset vom Metamodell beschreiben auf welchen die Traceability Architektur aufbaut
%     \item Pro Architektur ein Kapitel, welche die Funktionsweise beschreibt und wie es gedenkt die Anforderungen zu erfüllen
% \end{itemize}
% \hl{structure notes end}

Eine Regulation bildet ein Regelwerk wie z.B. \ac{si001} ab, welches die verschiedenen Controls enthält. Eine Control ist je eine Anforderung welche eingehalten werden muss damit die Control als eingehalten gilt. Eine Control kann durch eine oder mehrere Policies implementiert werden, welche die konkrete Umsetzung der Control darstellen. Diese Policies können entweder selbst definiert oder von den Cloud-Providern bereitgestellt werden. Die Traceability Architektur beschreibt, wie diese Controls und Policies organisiert und gebündelt werden, um die Anforderungen der Regulationen zu erfüllen und gleichzeitig eine effiziente Implementierung und Rückverfolgbarkeit zu gewährleisten.

\subsection{Variante 1: Control-centric Bundling}
\label{sec:trace-arch-variant1}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ip6-ideation-Traceability-Architecture-control-centric.drawio.pdf}
    \caption{Control-centric Traceability Architektur Variante}
    \label{fig:traceability-architecture-control-centric}
\end{figure}

\textbf{Ansatz}: Für jede Control in der jeweiligen Regulation wird ein Mapping zu einem Plattform-Bundle gemacht.

\textbf{Fokus}: Traceability, da die Controls direkt mit den Bundles verknüpft sind.

\textbf{Artefakte}: versionierte Bundles z.B. in Git, Governance Decisions und Evidence als Datenobjekte

\textbf{Funktionsweise}: In dieser Architektur-Variante werden die Controls direkt mit den Plattform-Bundles verknüpft. Das bedeutet, dass für jede Control in der Regulation ein spezifisches Mapping zu einem oder mehreren Bundles besteht, die die entsprechenden Policies enthalten. Beim Deployment einer \ac{lz} wählt das Governance Cockpit die benötigten Controls aus, basierend auf den Anforderungen der \ac{lz} (z.B. Schutzbedarf, Compliance-Anforderungen, etc.).

\textbf{Verantwortlichkeiten}: Der Projektverantwortliche definiert die Anforderungen für die jeweilige Foundation. Der Compliance Officer ist zuständig für die Regulations und Controls. Er prüft zudem das Mapping der Bundles durch den Cloud-Experten sowie die Zuweisung der Bundles zu den Foundations.

\textbf{Maintenance}: Da jedes Control direkt mit einem Bundle verknüpft ist, erfordert die Pflege und Aktualisierung der Controls eine sorgfältige Verwaltung der Bundles. Änderungen an einem Control können Auswirkungen auf mehrere Bundles haben, was eine Versionierung und koordinierte Aktualisierung erfordert.

\textbf{Adaptability}: Neue Controls können durch die Erstellung und Verknüpfung neuer Bundles hinzugefügt werden. Dies setzt jedoch einen soliden Lifecycle-Prozess voraus, sowohl auf Seite des \ac{iac} sowohl auch des Cloud-Providers. Die Komplexität der Auswahl der richtigen Bundles für eine \ac{lz} nimmt zu, wenn die Anzahl der Controls und Bundles wächst.

\textbf{Abgrenzung zu anderen Varianten}: Im Vergleich zu Variante 2, welche die Controls in Component-zentrierte Profiles bündelt, ist diese Variante stärker auf die direkte Verknüpfung von Controls mit Bundles fokussiert, was eine klarere Rückverfolgbarkeit ermöglicht. Variante 3 hingegen bündelt die Controls auf einer höheren Ebene, nämlich auf der Ebene von \ac{lz}-Kategorien, was zu einer stärkeren Abstraktion führt und die Rückverfolgbarkeit eher kompliziert.

\textbf{Tracebility-Strength}: Die Rückverfolgbarkeit zu einzelnen Controls ist in dieser Variante am stärksten, da die Controls direkt mit den Bundles verknüpft sind. Es ist einfach zu verfolgen, welche Bundles welche Controls implementieren, was eine klare Rückverfolgung ermöglicht.

\textbf{Laufzeitkomplexität}: Deploy Foundation: O(n), Verify Compliance: O(n)

% VERIFY COMPLIANCE
% @startuml

% participant "Cloud\nProvider\nAPI" as cloud
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Evidence\nService" as evidence
% actor "Compliance\nOfficer" as comp
% database "Governance\nDecision\nStore" as decision
% participant "Governance\nConfiguration\nService" as config

% == Verify compliance (Scope of UC 06 - Variant 1: Control-centric) ==

% trace --> project : Get LZ account IDs\nof Foundation
% activate trace
% trace --> decision : get Decision for Foundation
% trace --> trace: Get control IDs\nfrom Decision
% loop Control IDs
% trace --> config : get policies for\ncontrol ID
% config --> trace : policies
% trace --> cloud : get compliance state for Foundation
% cloud --> trace : compliant/uncompliant
% trace --> trace : create evidence for control ID
% trace --> evidence : store evidence
% end
% trace --> project: created evaluation snapshot
% deactivate trace
% project --> trace : get compliance for controls of project
% activate trace
% trace --> trace : aggregate latest evidence
% trace --> project : compliance result
% deactivate trace

% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant1_verify-compliance.png}
    \caption{Sequenz-Diagramm für Verifikation der Compliance nach Traceability Architektur Variante 1}
    \label{fig:methodology-poc-variant1-sequence-verification}
\end{figure}

% DEPLOY NEW FOUNDATION
% @startuml

% participant "Cloud\nProvider\nAPI" as cloud
% participant "LZ\nProvisioning\nService" as lz
% actor "Projekt-\nverantwortlicher" as po
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Governance\nConfiguration\nService" as config
% database "Governance\nDecision\nStore" as decision
% database "Governance\nIntent\nRepository" as intent

% == Deploy new LZ (Scope of UC 06 - Variant 1: Control-centric) ==

% po --> project : Declare choice for\nLZ Component category
% project --> trace : Get Foundation\nPlacement Decision
% activate trace
% note right of trace
% generic input:
% - cloud provider
% - environment
% - data requirements
% - RPO, backup strategy, availability requirements, ...
% end note
% trace --> config : get Control IDs\nbased on Input
% activate config
% loop Foundation Requirement Aspects
%     config --> intent : Check if Control\nBundles are available
% end
% alt matching LZ(s) found
%     config --> trace : Control IDs
% else no matching LZ found
%     config --> trace : empty List
% end
% deactivate config
% trace --> decision: Store Control IDs
% return LZ Foundation\nPlacement Decision
% project --> decision : get required Policies
% project --> lz : Create LZ with required Policies
% lz --> cloud: Create Foundation\nbased on Blueprint

% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant1_deploy-foundation.png}
    \caption{Sequenz-Diagramm für das Deployment einer neuen LZ nach Traceability Architektur Variante 2}
    \label{fig:methodology-poc-variant1-sequence-deployment}
\end{figure}

\textbf{Pros}
\begin{itemize}
    \item Rückverfolgbarkeit ist trivial: Das Ergebnis des Enforcements wird direkt einer Control-ID zugeordnet.
    \item Ideal für Audits (\flqq Zeig mir alle implementierten Controls\frqq).
\end{itemize}

\textbf{Cons}
\begin{itemize}
    \item Viele kleine Artefakte → erfordern Automatisierung und eine Build-Pipeline
    \item Einige Provider bevorzugen grössere Sets
\end{itemize}

\textbf{Fazit}: Diese Variante ist diejenige welche am besten die Traceability Anforderungen erfüllt, aber auch die aufwändigste in der Umsetzung ist. Es könnte als \flqq Goldstandard\frqq\ für die Traceability Architektur angesehen werden, aber es muss evaluiert werden ob der Aufwand gerechtfertigt ist im Vergleich zu den anderen Varianten.

\subsection{Variante 2: LZ component-centric Bundling}
\label{sec:trace-arch-variant2}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ip6-ideation-Traceability-Architecture-component-centric.drawio.pdf}
    \caption{Component-centric Traceability Architektur Variante}
    \label{fig:traceability-architecture-component-centric}
\end{figure}

\textbf{Ansatz}: Die Controls werden nach \ac{lz}-Komponenten in Component-Profiles gebündelt.

\textbf{Fokus}: Engineering, Verteilung an Expertengruppen/Teams, aber mit Rückverfolgbarkeit

\textbf{Artefakte}: versionierte Component-Profiles z.B. in Git, Governance Decisions und Evidence als Datenobjekte

\textbf{Funktionsweise}: Die Controls haben mind. eine Beziehung zu mind. einer \ac{lz}-Komponente, welche die Control implementiert. Für jede \ac{lz}-Komponente existieren verschiedene Profiles, diese Profiles bauen aufeinander auf, heisst die Nächsthöhere enthält Vorherige auch. Ein Profile kann jeweils mehrere Controls implementieren und bildet das Mapping zwischen Controls und konkreten Policies auf der jeweiligen Zielplattform. Policies können sowohl selbst definiert als auch vom Provider bereitgestellt sein. Der Cloud-Experte ist zuständig für das Mapping der Component-Profiles auf die Controls.

\textbf{Verantwortlichkeiten}: Der Projektverantwortliche definiert die Anforderungen für die jeweilige Foundation. Der Compliance Officer ist zuständig für die Regulations und Controls. Er prüft zudem das Mapping der Component-Profiles durch den Cloud-Experten sowie die Zuweisung der Component-Profiles zu den Foundations.

\textbf{Maintenance}: In dieser Architektur-Variante werden die Controls in Component Profiles gebündelt, folgend erfordern Änderungen an einer Control nur die Aktualisierung des entsprechenden Profiles, welche versionierbar sind. Durch die Versionierbarkeit kann z.B. ein Compliance-Officer neue Versionen der Component-Profiles erstellen, ohne dass die bestehenden Foundations direkt betroffen sind. Sobald eine neue Version eines Component-Profils freigegeben ist, können die entsprechenden Foundations und die übergreifende Landing-Zone-Components per Provisionierung migriert werden.

\textbf{Adaptability}: Neue Controls können durch die Erstellung und Verknüpfung neuer Versionen von Profiles hinzugefügt werden. Dies ermöglicht eine flexible Anpassung an neue Anforderungen, da die Profiles auf Komponentenebene organisiert sind und Änderungen an einer Control nicht zwangsläufig Auswirkungen auf andere Controls oder Bundles haben.

\textbf{Abgrenzung zu anderen Varianten}: Änhlich wie in Variante 1 werden hier Controls \flqq gebundelt\frqq\ aber anstatt in Control-zentrierte Bundles, werden sie in Component-zentrierte Profiles gebündelt. Die Profiles sind somit eher nach technischen Komponenten organisiert, welche von Expertengruppen/Teams implementiert werden, anstatt direkt nach Controls. Variante 3 im Gegenzug bündelt die Controls auf einer noch höheren Ebene, nämlich auf der Ebene von \ac{lz}-Kategorien, was zu einer stärkeren Abstraktion führt.

\textbf{Tracebility-Strength}: Die Traceability ist durch die Verknüpfung der Controls mit den Component Profiles gegeben, da jedes Profile die spezifischen Controls enthält, die es implementiert. Allerdings erfordert die Rückverfolgbarkeit zu einzelnen Controls einen zusätzlichen Suchschritt, da die Controls in den Profiles gebündelt sind. Es ist notwendig, ein Manifest zu führen, das auflistet, welche Controls in welchen Component-Profilen enthalten sind, um eine klare Rückverfolgung zu ermöglichen.

\textbf{Laufzeitkomplexität}: Deploy Foundation: O(n), Verify Compliance: O(n)

% VERIFY COMPLIANCE
% @startuml

% participant "Cloud\nProvider\nAPI" as cloud
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Evidence\nService" as evidence
% actor "Compliance\nOfficer" as comp
% database "Governance\nDecision\nStore" as decision
% participant "Governance\nConfiguration\nService" as config

% == Verify compliance (Scope of UC 06 - Variant 2: Component-centric) ==

% trace --> project : Get LZ account IDs\nof Foundation
% activate trace
% trace --> decision : get Decision for Foundation
% trace --> trace: Get control IDs\nfrom Decision
% loop Component Profile IDs
% trace --> config : get component profile
% config --> trace : component profile
% trace --> config : get policies for component profile
% config --> trace : policies
% trace --> cloud : get compliance state for Foundation
% cloud --> trace : compliant/uncompliant
% trace --> trace : create evidence for control ID
% trace --> evidence : store evidence
% end
% trace --> project: created evaluation snapshot
% deactivate trace
% project --> trace : get compliance for controls of project
% activate trace
% trace --> trace : aggregate latest evidence
% trace --> project : compliance result
% deactivate trace

% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant2_verify-compliance.png}
    \caption{Sequenz-Diagramm für Verifikation der Compliance nach Traceability Architektur Variante 2}
    \label{fig:methodology-poc-variant2-sequence-verification}
\end{figure}

% DEPLOY NEW FOUNDATION
% @startuml
% participant "Cloud\nProvider\nAPI" as cloud
% participant "LZ\nProvisioning\nService" as lz
% actor "Projekt-\nverantwortlicher" as po
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Governance\nConfiguration\nService" as config
% database "Governance\nDecision\nStore" as decision
% database "Governance\nIntent\nRepository" as intent

% == Deploy new LZ (Scope of UC 06 - Variant 2: Component-centric) ==

% po --> project : Declare choice for\nLZ Component category
% project --> trace : Get Foundation\nPlacement Decision
% activate trace
% note right of trace
% generic input:
% - cloud provider
% - environment
% - data requirements
% - RPO, backup strategy, availability requirements, ...
% end note
% trace --> config : Get LZ Governance\nDomain ID
% activate config
% loop Foundation Requirement Aspects
%     config --> config : Identify specific\nComponent Profile
% end
% config --> config : Determine minimal Required\nComponent Profile Set
% config --> config : Collect Component Profiles
% alt matching LZ(s) found
%     config --> trace : List of\nComponent Profiles
% else no matching LZ found
%     config --> trace : empty List
% end
% deactivate config
% trace --> decision: Store Foundation Placement Decision
% return LZ Foundation\nPlacement Decision
% project --> decision : get required Policies
% project --> lz : Create LZ with required Policies
% lz --> cloud: Create Foundation\nbased on Blueprint
% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant2_deploy-foundation.png}
    \caption{Sequenz-Diagramm für das Deployment einer neuen LZ nach Traceability Architektur Variante 2}
    \label{fig:methodology-poc-variant2-sequence-deployment}
\end{figure}

\textbf{Pros}
\begin{itemize}
    \item Passt zur Implementierungspraxis von Teams: Netzwerkrichtlinien werden gemeinsam verwaltet
    \item Weniger Bundles, einfachere Einführung
\end{itemize}

\textbf{Cons}
\begin{itemize}
    \item Die Rückverfolgbarkeit erfordert einen zusätzlichen Suchschritt: \flqq Richtlinie X befindet sich im Bundle Netzwerk → enthält Controls A, B, C\frqq
    \item Einige Policies implementieren mehrere Controls (1:n)
\end{itemize}

\textbf{Fazit}: Diese Variante bietet einen guten Kompromiss zwischen Rückverfolgbarkeit und Praktikabilität, da sie die Implementierungspraxis der Teams berücksichtigt, aber dennoch eine gewisse Rückverfolgbarkeit ermöglicht. Es könnte als \flqq Silberstandard\frqq\ für die Traceability Architektur angesehen werden, da es eine praktikable Lösung darstellt, ohne den Aufwand der Control-zentrierten Variante zu erfordern.

\subsection{Variante 3: LZ category centric Bundling}
\label{sec:trace-arch-variant3}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ip6-ideation-Traceability-Architecture-category-centric.drawio.pdf}
    \caption{LZ Category centric Traceability Architektur Variante}
    \label{fig:traceability-architecture-category-centric}
\end{figure}

\textbf{Ansatz}: Controls werden nach \ac{lz}-Profiles gebündelt, welche die Rules für alle Komponenten enthalten. Diese \ac{lz}-Profiles umfassen die entsprechenden Policies. Beim deployen einer \ac{lz} wählt das Governance Cockpit nur die \ac{lz}-Profiles aus, der Rest wird abstrahiert.

\textbf{Fokus}: LZ-Kategorien mit Vereinfachung für den Endnutzer, da die Profile alles umfasst, ohne dass viel ausgewählt werden muss.

\textbf{Artefakte}: versionierte \ac{lz}-Profiles z.B. in Git, Governance Decisions und Evidence als Datenobjekte

\textbf{Funktionsweise}: Die Controls werden in \ac{lz}-Profiles gebündelt, die alle relevanten Regeln für die verschiedenen Komponenten enthalten. Jedes Profile repräsentiert eine bestimmte \ac{lz}-Kategorie, die eine Risikoklasse oder einen Anwendungsfall abdeckt. Beim Deployen einer \ac{lz} wählt das Governance Cockpit das entsprechende Profile aus, ohne dass der Benutzer sich mit den einzelnen Controls oder Policies auseinandersetzen muss. Diese Herangehensweise bietet eine einfache Anwendung und eine klare Customer Story, da die Kategorien direkt mit den Risikoklassen verknüpft sind. Allerdings führt dies zu einer hohen Duplikation von Controls über verschiedene Kategorien hinweg, und die Rückverfolgbarkeit zu einzelnen Controls erfordert die Pflege eines Manifests, das auflistet, welche Controls in welchen Profilen enthalten sind.

\textbf{Verantwortlichkeiten}: Der Projektverantwortliche definiert die Anforderungen für die jeweilige Foundation. Der Compliance Officer ist zuständig für die Regulations und Controls. Er prüft zudem das Mapping der \ac{lz}-Profiles durch den Cloud-Experten sowie die Zuweisung der \ac{lz}-Profiles zu den Foundations.

\textbf{Maintenance}: Änderungen an einer Control erfordern die Aktualisierung aller \ac{lz}-Profiles, die diese Control enthalten, was den Wartungsaufwand erhöht. Es ist wichtig, einen klaren Versionierungsprozess zu etablieren, um sicherzustellen, dass Änderungen nachvollziehbar und Versionen referenzierbar sind und die Historie der Controls und \ac{lz}-Profiles gepflegt wird. Dies könnte durch die Verwendung von \ac{iac} in Git erreicht werden, um die Änderungen zu verfolgen und Versionierung von Controls und Profiles zu ermöglichen.

\textbf{Adaptability}: Neue Controls können durch die Erstellung und Verknüpfung neuer Versionen von \ac{lz}-Profiles hinzugefügt werden. Allerdings kann dies zu einer erhöhten Komplexität führen, da die Duplikation von Controls über verschiedene Kategorien hinweg die Verwaltung und Aktualisierung der \ac{lz}-Profiles erschwert.

\textbf{Abgrenzung zu anderen Varianten}: Im Vergleich zu Variante 1 und 2, welche die Controls in Bundles oder Component-zentrierte Profiles bündeln, bündelt diese Variante die Controls auf einer höheren Ebene, nämlich auf der Ebene von \ac{lz}-Kategorien.

\textbf{Tracebility-Strength}: Die Rückverfolgbarkeit zu einzelnen Controls ist in dieser Variante am schwierigsten, da die Controls in den \ac{lz}-Profiles gebündelt sind und es eine hohe Duplikation von Controls über verschiedene Kategorien hinweg gibt. Es erfordert die Pflege eines Manifests, das auflistet, welche Controls in welchen \ac{lz}-Profiles enthalten sind, um eine klare Rückverfolgung zu ermöglichen.

\textbf{Laufzeitkomplexität}: Deploy Foundation: O(n), Verify Compliance: O(n)

% VERIFY COMPLIANCE
% @startuml

% participant "Cloud\nProvider\nAPI" as cloud
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Evidence\nService" as evidence
% actor "Compliance\nOfficer" as comp
% database "Governance\nDecision\nStore" as decision
% participant "Governance\nConfiguration\nService" as config

% == Verify compliance (Scope of UC 06 - Variant 3: Category-centric) ==

% trace --> project : Get LZ account IDs\nof Foundation
% activate trace
% trace --> decision : get Decision for Foundation
% trace --> trace: Get category ID\nfrom Decision
% trace --> config : Get policies for category
% config --> trace : policies
% loop Policies
% trace --> cloud : get compliance state for Foundation
% cloud --> trace : compliant/uncompliant
% trace --> trace : create evidence for control ID
% trace --> evidence : store evidence for policy
% end
% trace --> project : created evaluation snapshot
% deactivate trace
% project --> trace : get compliance for project
% activate trace
% trace --> trace : aggregate latest evidence
% trace --> project : compliance result
% deactivate trace

% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant3_verify-compliance.png}
    \caption{Sequenz-Diagramm für Verifikation der Compliance nach Traceability Architektur Variante 3}
    \label{fig:methodology-poc-variant3-sequence-verification}
\end{figure}

% DEPLOY NEW FOUNDATION
% @startuml
% participant "Cloud\nProvider\nAPI" as cloud
% participant "LZ\nProvisioning\nService" as lz
% actor "Projekt-\nverantwortlicher" as po
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Governance\nConfiguration\nService" as config
% database "Governance\nDecision\nStore" as decision
% database "Governance\nIntent\nRepository" as intent

% == Deploy new LZ (Scope of UC 06 - Variant 3: Category-centric) ==

% po --> project : Declare choice for\nLZ Component category
% project --> trace : Get Foundation\nPlacement Decision
% activate trace
% note right of trace
% generic input:
% - cloud provider
% - environment
% - data requirements
% - RPO, backup strategy, availability requirements, ...
% end note
% trace --> config : Get LZ Governance\nDomain ID
% activate config
% loop Foundation Requirement Aspects
%     config --> config : Aggregate to\nCategory Decision
% end
% config --> config : Determine Category
% alt matching LZ(s) found
%     config --> trace : Foundation Category
% else no matching LZ found
%     config --> trace : no Category matched
% end
% deactivate config
% trace --> decision: Store Foundation Placement Decision
% return Foundation\nCategory
% project --> decision : get required Policies
% project --> lz : Create LZ with required Policies
% lz --> cloud: Create Foundation\nbased on Blueprint
% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant3_deploy-foundation.png}
    \caption{Sequenz-Diagramm für das Deployment einer neuen LZ nach Traceability Architektur Variante 3}
    \label{fig:methodology-poc-variant3-sequence-deployment}
\end{figure}

\textbf{Pros}
\begin{itemize}
    \item Extrem einfache Anwendung (\flqq regulierte Profile anwenden\frqq)
    \item Klare Customer Story: Kategorien repräsentieren Risikoklassen
\end{itemize}

\textbf{Cons}
\begin{itemize}
    \item Hohe Duplikation von Controls über Kategorien hinweg
    \item Rückverfolgbarkeit zu einzelnen Controls ist gegeben, aber man muss ein Manifest \flqq Controls in der Profile\frqq\ führen
\end{itemize}

\textbf{Fazit}: Diese Variante bietet die einfachste Anwendung für den Endnutzer, da sie die Komplexität der Auswahl von Controls und Policies abstrahiert. Es könnte als \flqq Bronze-Standard\frqq\ für die Traceability Architektur angesehen werden, da es eine benutzerfreundliche Lösung darstellt, aber mit erheblichen Nachteilen in Bezug auf Duplikation und Rückverfolgbarkeit einhergeht.

\subsection{Variante 4: LZ Provider-native Bundling}
\label{sec:trace-arch-variant4}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{ip6-ideation-Traceability-Architecture-provider-native.drawio.pdf}
    \caption{LZ Provider Native Traceability Architektur Variante}
    \label{fig:traceability-architecture-provider-native}
\end{figure}

\textbf{Ansatz}: Controls werden direkt durch die native Policy-Abstraction des Providers dargestellt (Azure Initiatives, AWS SCP usw.). Diese werden direkt auf die Komponenten angewendet.

\textbf{Fokus}: Die Anwendung von Richtlinien auf Ressourcen, weniger auf die Nachvollziehbarkeit (à la MVP). Initiatives/Bundles existieren bereits von verschiedenen Cloud-Providern.

\textbf{Artefakte}: versionierte Richtlinien in Git, Governance Decisions und Evidence als Datenobjekte

\textbf{Funktionsweise}: In dieser Architektur-Variante werden die Controls direkt durch die nativen Richtlinienabstraktionen der Cloud-Provider dargestellt. Das bedeutet, dass die Implementierung der Controls direkt auf den Ressourcen erfolgt, ohne dass eine zusätzliche Abstraktionsschicht wie Bundles oder Profiles erforderlich ist. Diese Herangehensweise ermöglicht eine optimale Leistung und eine enge Ausrichtung an den Mechanismen der Provider, da sie die nativen Funktionen und Tools nutzt. Allerdings führt dies zu einer schlechteren Vergleichbarkeit über verschiedene Cloud-Provider hinweg, da jeder Provider unterschiedliche Formate und Strukturen für seine Richtlinien hat.

\textbf{Verantwortlichkeiten}: Der Projektverantwortliche definiert die Anforderungen für die jeweilige Foundation. Der Compliance Officer ist zuständig für die Regulations und Controls. Er prüft zudem die Implementierung der Controls durch den Cloud-Provider bzw. den Cloud-Experten, welcher direkt die nativen Richtlinien des Providers auf die Ressourcen anwendet.

\textbf{Maintenance}: Da die Controls direkt auf den Ressourcen implementiert werden, erfordert die Wartung und Aktualisierung der Controls eine sorgfältige Verwaltung der Ressourcen und ihrer zugehörigen Richtlinien. Es ist wichtig, einen klaren Versionierungsprozess zu etablieren, um sicherzustellen, dass Änderungen nachvollziehbar und Versionen referenzierbar sind und die Historie der Controls gepflegt wird. Dies könnte durch die Verwendung von \ac{iac} in Git erreicht werden, um die Änderungen zu verfolgen und Versionierung von Ressourcen und Richtlinien zu ermöglichen.

\textbf{Adaptability}: Neue Controls können durch die Erstellung und Verknüpfung neuer Ressourcen und Richtlinien hinzugefügt werden. Allerdings kann dies zu einer erhöhten Komplexität führen, da die Verwaltung der Ressourcen und ihrer zugehörigen Richtlinien über verschiedene Cloud-Provider hinweg eine Herausforderung darstellen kann.

\textbf{Abgrenzung zu anderen Varianten}: Im Vergleich zu den anderen Varianten, welche die Controls in Bundles oder Profiles bündeln, bündelt diese Variante die Controls nicht in einer zusätzlichen Abstraktionsschicht, sondern implementiert sie direkt auf den Ressourcen. Dies führt zu einer engeren Ausrichtung an den Mechanismen der Provider, aber auch zu Herausforderungen in Bezug auf Vergleichbarkeit und Rückverfolgbarkeit.

\textbf{Tracebility-Strength}: Die Rückverfolgbarkeit zu einzelnen Controls ist in dieser Variante vergleichsweise schlecht, da die Controls direkt auf den Ressourcen implementiert werden und es keine zusätzliche Abstraktionsschicht gibt, die die Controls organisiert. Es erfordert eine Normalisierung über die verschiedenen Evidenceformate der Provider hinweg, um sicherzustellen, dass die Controls konsistent identifiziert und verfolgt werden können.

\textbf{Laufzeitkomplexität}: Deploy Foundation: O(n), Verify Compliance: O(1) bis O(n)

% VERIFY COMPLIANCE
% @startuml

% participant "Cloud\nProvider\nAPI" as cloud
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Evidence\nService" as evidence
% actor "Compliance\nOfficer" as comp
% database "Governance\nDecision\nStore" as decision
% participant "Governance\nConfiguration\nService" as config

% == Verify compliance (Scope of UC 06 - Variant 4: Provider-native) ==

% trace --> project : Get LZ account IDs\nof Foundation
% activate trace
% trace --> decision : get Decision for Foundation
% trace --> trace: Get governance domain\nfrom Decision
% trace --> cloud : get compliance state for Foundation
% cloud --> trace : compliant/uncompliant
% trace --> trace : create evidence for Foundation
% trace --> evidence : store evidence
% trace --> project: created evaluation snapshot
% deactivate trace
% project --> trace : get compliance for project
% activate trace
% trace --> trace : aggregate latest evidence
% trace --> project : compliance result
% deactivate trace

% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant4_verify-compliance.png}
    \caption{Sequenz-Diagramm für Verifikation der Compliance nach Traceability Architektur Variante 4}
    \label{fig:methodology-poc-variant4-sequence-verification}
\end{figure}

% DEPLOY NEW FOUNDATION
% @startuml
% participant "Cloud\nProvider\nAPI" as cloud
% participant "LZ\nProvisioning\nService" as lz
% actor "Projekt-\nverantwortlicher" as po
% participant "Project\nService" as project
% participant "Traceability\nService" as trace
% participant "Governance\nConfiguration\nService" as config
% database "Governance\nDecision\nStore" as decision
% database "Governance\nIntent\nRepository" as intent

% == Deploy new LZ (Scope of UC 06 - Variant 4: Provider-native) ==

% po --> project : Declare choice for\nLZ Component category
% project --> trace : Get Foundation\nPlacement Decision
% activate trace
% note right of trace
% generic input:
% - cloud provider
% - environment
% - data requirements
% - RPO, backup strategy, availability requirements, ...
% end note
% trace --> config : Get LZ Governance\nDomain ID
% activate config
% config --> config : Determine Cloud Provider
% loop Foundation Requirement Aspects
%     config --> config : Aggregate to Governance Domain
% end
% config --> config : Determine Governance Domain
% alt matching LZ(s) found
%     config --> trace : Governance Domain
% else no matching LZ found
%     config --> trace : empty List
% end
% deactivate config
% trace --> decision: Store Foundation Placement Decision
% return Governance\nDomain
% project --> lz : Create LZ in Governance Domain
% lz --> cloud: Create Foundation\nbased on Blueprint
% @enduml

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{graphics/sequence-diagramm-poc_variant4_deploy-foundation.png}
    \caption{Sequenz-Diagramm für das Deployment einer neuen LZ nach Traceability Architektur Variante 4}
    \label{fig:methodology-poc-variant4-sequence-deployment}
\end{figure}

\textbf{Pros}
\begin{itemize}
    \item Operativ sauber: Nutzung der nativen Mechanismen der Provider
    \item Beste Leistung und Providerausrichtung
\end{itemize}

\textbf{Cons}
\begin{itemize}
    \item Cloud-übergreifende Vergleichbarkeit wird schlechter
    \item Traceability muss über verschiedene Evidenceformate der Provider hinweg normalisiert werden
\end{itemize}

\textbf{Fazit}: Diese Variante bietet die beste Leistung und Providerausrichtung, da sie die nativen Mechanismen der Cloud-Provider nutzt. Es könnte als \flqq Performance-Standard\frqq\ für die Traceability Architektur angesehen werden, da sie die effizienteste Nutzung der Ressourcen ermöglicht, aber mit erheblichen Herausforderungen in Bezug auf Vergleichbarkeit und Rückverfolgbarkeit einhergeht.
